const fs = require('fs-extra')

exports.onPreInit = () => {
  console.log('>>>> ultronele demo pre build...');
  // let folder = './cache';
  // fs.emptyDirSync(folder);

  // chech contentPath existence
  
}
